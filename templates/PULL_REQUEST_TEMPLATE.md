## What changed

## Test added / changed
<!-- Required when touching app/, components/, i18n, vercel.json. Path to the spec: -->
- e2e/pages/…spec.ts

## Checklist
- [ ] New page → new `e2e/pages/<route>.spec.ts` (CI fails otherwise)
- [ ] Bug fix → failing test committed before the fix
- [ ] Feature flag → both states covered
- [ ] Copy change → old phrase grep is empty repo-wide
